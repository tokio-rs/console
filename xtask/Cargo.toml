[package]
name = "xtask"
version = "0.1.0"
license = "MIT"
edition = "2021"
rust-version = "1.56.0"
publish = false

[dependencies]
tonic-build = { version = "0.8", default-features = false, features = [
    "prost", "transport"
] }
clap = { version = "4", features = ["derive"] }
color-eyre = "0.6"