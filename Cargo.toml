[workspace]
members = [
    "tokio-console",
    "console-subscriber",
    "console-api",
    "xtask"
]
resolver = "2"

# The profile that 'cargo dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
